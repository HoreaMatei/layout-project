import { type Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        coolvetica: ["Coolvetica", "sans-serif"],
      },
    },
  },
};

export default config;
